# Local API (default in code if unset: https://localhost:7156)
# VITE_API_URL=https://localhost:7156

# Production build (`npm run build`) loads `.env.production` — see that file for the live Azure host.
